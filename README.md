# OPNSense HA Failovers

## OPNSense setup

You need to make a route to the VIP disable it, and get the routes ID via the developer tools in the browser.

You also have to make a gateway specifically to the host machine.  This can be shared across multiple VIPs (?)

You need to make peers, and get their IDs